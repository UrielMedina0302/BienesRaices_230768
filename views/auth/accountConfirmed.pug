extends ../layout/index

block contenido 
    .py-10 
        h1.text-center.text-4xl.my-10.font-extrabold.text-dark-cyan Bienes 
            span.font-normal.text-white Raices
        h2.text-center.text-tx2= page

    div(class="bg-white shadow py-8 px-6 rounded-lg max-w-2xl mx-auto font-bold text-white uppercase")
        p(class= error ? 'bg-red-500': 'bg-green-500').py-2.px-5.rounded-1g.w-full.font-bold.text-center.text-white.uppercase= msg

        if(!error)
            a(href="/auth/login/" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes iniciar sesión